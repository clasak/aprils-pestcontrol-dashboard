import { Box, Typography } from '@mui/material';

const DispatchPage = () => {
  return (
    <Box>
      <Typography variant="h4" gutterBottom>
        Dispatch Board
      </Typography>
      <Typography variant="body1" color="text.secondary">
        Dispatch and assignment interface coming soon...
      </Typography>
    </Box>
  );
};

export default DispatchPage;
